<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.svyat</groupId>
    <artifactId>pinterest-board-downloader</artifactId>
    <version>1.0.0</version>
    <name>Pinterest Board Downloader</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <maven.compiler.release>25</maven.compiler.release>

        <javafx.version>25.0.4</javafx.version>
        <jackson.version>2.22.1</jackson.version>

        <main.class>com.svyat.pinterestdownloader.Launcher</main.class>
        <app.name>PinterestBoardDownloader</app.name>
        <app.version>1.0.0</app.version>

        <jpackage.input>${project.build.directory}/jpackage-input</jpackage.input>
        <jpackage.output>${project.build.directory}/dist</jpackage.output>
        <jpackage.type>app-image</jpackage.type>
        <jpackage.name>${app.name}</jpackage.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>${app.name}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>public</directory>
                <includes>
                    <include>pinterest.png</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${main.class}</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.8.1</version>
                <executions>
                    <execution>
                        <id>copy-runtime-dependencies</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeScope>runtime</includeScope>
                            <outputDirectory>${jpackage.input}</outputDirectory>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
                <executions>
                    <execution>
                        <id>stage-main-jar</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${jpackage.input}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}</directory>
                                    <filtering>false</filtering>
                                    <includes>
                                        <include>${app.name}.jar</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>build-native-package</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${java.home}/bin/jpackage</executable>
                            <arguments>
                                <argument>--type</argument>
                                <argument>${jpackage.type}</argument>

                                <argument>--name</argument>
                                <argument>${jpackage.name}</argument>

                                <argument>--app-version</argument>
                                <argument>${app.version}</argument>

                                <argument>--vendor</argument>
                                <argument>Svyat</argument>

                                <argument>--description</argument>
                                <argument>Pinterest Board Downloader</argument>

                                <argument>--input</argument>
                                <argument>${jpackage.input}</argument>

                                <argument>--dest</argument>
                                <argument>${jpackage.output}</argument>

                                <argument>--main-jar</argument>
                                <argument>${app.name}.jar</argument>

                                <argument>--main-class</argument>
                                <argument>${main.class}</argument>

                                <argument>--java-options</argument>
                                <argument>-Dfile.encoding=UTF-8</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
            </activation>
            <properties>
                <jpackage.type>app-image</jpackage.type>
                <jpackage.name>${app.name}</jpackage.name>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>prepare-native-package-clean</id>
                                <phase>pre-clean</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>attrib</executable>
                                    <arguments>
                                        <argument>-R</argument>
                                        <argument>${jpackage.output}/${jpackage.name}/${jpackage.name}.exe</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>build-native-package</id>
                                <configuration>
                                    <arguments combine.children="append">
                                        <argument>--icon</argument>
                                        <argument>${project.basedir}/public/pinterest.ico</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>finalize-native-package</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>attrib</executable>
                                    <arguments>
                                        <argument>-R</argument>
                                        <argument>${jpackage.output}/${jpackage.name}/${jpackage.name}.exe</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>macos</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <jpackage.type>dmg</jpackage.type>
                <jpackage.name>${app.name}</jpackage.name>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>build-native-package</id>
                                <configuration>
                                    <arguments combine.children="append">
                                        <argument>--mac-package-identifier</argument>
                                        <argument>com.svyat.pinterestboarddownloader</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>debian</id>
            <activation>
                <os>
                    <name>Linux</name>
                </os>
            </activation>
            <properties>
                <jpackage.type>deb</jpackage.type>
                <jpackage.name>${project.artifactId}</jpackage.name>
            </properties>
        </profile>
    </profiles>
</project>
